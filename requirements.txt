# Core
streamlit
requests
python-dotenv
pydantic
typing-extensions

# LangGraph for graph-based chatbot state management
langgraph

# LangChain core & community tools
langchain
langchain-community
langchain-google-genai

# Optional / prebuilt tools (DuckDuckGo, ToolNode, etc.)
transformers
torch
sentence-transformers

# SQLite (for database checkpointing, built-in with Python; optional package for safety)
pysqlite3-binary


# Streamlit for UI
streamlit

# UUID is built-in, no need to install

# LangChain core messages
langchain-core

# Optional utilities if you call APIs inside backend_database
requests
